
위코드를
```C#
            // my-awesome-check
            var myAwesomeCheck = new CheckerItem("my-awesome-check");
            // 공통 옵션 (템플릿 규칙에 따라 자동 추가)
            myAwesomeCheck.Options.Add(new BoolOptionItem("OnlyMainFile", true)); // C++ Default: true
            myAwesomeCheck.Options.Add(new BoolOptionItem("IgnoreSystemHeaders", true)); // C++ Default: true
            myAwesomeCheck.Options.Add(new BoolOptionItem("IgnoreMacroExpansions", true)); // C++ Default: true
            
            // 추출된 옵션
            myAwesomeCheck.Options.Add(new BoolOptionItem("AnalyzeTemplate", true)); // C++ Default: true
            myAwesomeCheck.Options.Add(new UnsignedOptionItem("MaxDepth", 10)); // C++ Default: 10
            
            checkers.Add(myAwesomeCheck);
```

다음과 같이 변환