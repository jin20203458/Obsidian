| **체커 이름 (ARQAModule.cpp 기준)**                | **옵션 이름**                       | **기본값**       | **설명**                                        |
| -------------------------------------------- | ------------------------------- | ------------- | --------------------------------------------- |
| **lex-include-charset**                      | OnlyMainFile                    | `true`        | 메인 파일만 검사할지 여부                                |
|                                              | IgnoreSystemHeaders             | `true`        | 시스템 헤더 무시 여부                                  |
|                                              | AllowHyphen                     | `false`       | `#include` 경로에 하이픈('-') 허용 여부                 |
|                                              | AllowUnicode                    | `false`       | `#include` 경로에 비ASCII 문자 허용 여부                |
|                                              | AllowSpaces                     | `false`       | `#include` 경로에 공백 문자 허용 여부                    |
|                                              | AllowParentPath                 | `false`       | `#include` 경로에 부모 경로("..") 허용 여부              |
|                                              | AllowDoubleSep                  | `false`       | `#include` 경로에 연속된 구분자("//", "\\") 허용 여부      |
|                                              | AllowAbsolutePath               | `false`       | `#include` 경로에 절대 경로 허용 여부                    |
|                                              | MaxPathLength                   | `260U`        | `#include` 경로의 최대 길이                          |
|                                              | EnforceForwardSlashOnly         | `true`        | `#include` 경로에 슬래시(/)만 허용할지 여부                |
| **lex-macro-defined-before-use**             | OnlyMainFile                    | `true`        | 메인 파일만 검사할지 여부                                |
|                                              | IgnoreSystemHeaders             | `true`        | 시스템 헤더 무시 여부                                  |
|                                              | WarnOnUndefined                 | `true`        | 정의되지 않은 매크로 사용 시 경고 여부                        |
|                                              | IgnoreBuiltinMacros             | `true`        | 컴파일러 내장 매크로(`__FILE__` 등) 무시 여부               |
| **ast-aggregate-init-style**                 | OnlyMainFile                    | `true`        | 메인 파일만 검사할지 여부                                |
|                                              | IgnoreSystemHeaders             | `true`        | 시스템 헤더 무시 여부                                  |
|                                              | IgnoreMacroExpansions           | `true`        | 매크로 확장 결과 무시 여부                               |
|                                              | AllowEmptyBracesZeroInit        | `true`        | `{}`를 전체 0 초기화로 허용할지 여부                       |
|                                              | RequireNestedBracesForSubarrays | `true`        | 배열의 하위 요소가 집합(배열/구조체)일 때 중첩 중괄호 요구 여부         |
|                                              | RequireFullElementsForArrays    | `true`        | 배열 초기화 시 모든 요소를 명시하도록 요구 여부                   |
|                                              | RequireFullBracesForStruct      | `true`        | 구조체 초기화 시 모든 필드를 명시하도록 요구 여부                  |
| **ast-always-constant-condition**            | OnlyMainFile                    | `true`        | 메인 파일만 검사할지 여부                                |
|                                              | IgnoreSystemHeaders             | `true`        | 시스템 헤더 무시 여부                                  |
|                                              | IgnoreMacroExpansions           | `true`        | 매크로 확장 결과 무시 여부                               |
|                                              | EnableConstantFoldPhase         | `true`        | 상수 접기 기반 판정 활성화 여부                            |
|                                              | EnableTypeRangePhase            | `true`        | 타입 범위 기반 판정 활성화 여부                            |
|                                              | EnablePathConstraintPhase       | `true`        | 경로 제약 기반 판정 활성화 여부                            |
|                                              | OnlyIntegerComparisons          | `true`        | 경로 제약 분석 시 정수 비교만 고려할지 여부                     |
|                                              | UseParentDominatorOnly          | `true`        | 경로 제약 분석 시 직상위 분기만 고려할지 여부                    |
|                                              | MaxConstraintDepth              | `3`           | 경로 제약 분석 시 최대 중첩 깊이                           |
|                                              | ReportInElseBranches            | `true`        | `else` 블록 내부에서도 보고할지 여부                       |
|                                              | AnalyzeTernary                  | `true`        | 삼항 연산자(?:) 조건식도 분석할지 여부                       |
| **ast-array-index-bound**                    | OnlyMainFile                    | `true`        | 메인 파일만 검사할지 여부                                |
|                                              | IgnoreSystemHeaders             | `true`        | 시스템 헤더 무시 여부                                  |
|                                              | IgnoreMacroExpansions           | `true`        | 매크로 확장 결과 무시 여부                               |
|                                              | TrackIntraBlockConstants        | `true`        | 같은 블록 내 상수 값 추적 여부                            |
|                                              | MaxScanStmts                    | `600U`        | 블록 내 상수 추적 시 최대 스캔 문장 수                       |
| **ast-bool-logic-only-ops**                  | OnlyMainFile                    | `true`        | 메인 파일만 검사할지 여부                                |
|                                              | IgnoreSystemHeaders             | `true`        | 시스템 헤더 무시 여부                                  |
|                                              | IgnoreMacroExpansions           | `true`        | 매크로 확장 결과 무시 여부                               |
|                                              | ForbidEqualityComparisonsOnBool | `true`        | `bool` 타입에 대해 `==`, `!=` 비교도 금지할지 여부          |
| **ast-char-array-string-size**               | OnlyMainFile                    | `true`        | 메인 파일만 검사할지 여부                                |
|                                              | IgnoreSystemHeaders             | `true`        | 시스템 헤더 무시 여부                                  |
|                                              | IgnoreMacroExpansions           | `true`        | 매크로 확장 결과 무시 여부                               |
|                                              | AllowNullTerminatedMatch        | `false`       | 배열 크기가 문자열 길이와 같아도 허용할지 (널 종료 불가)             |
|                                              | RequireExactSize                | `false`       | 배열 크기를 문자열 길이 + 1 로 정확히 요구할지                  |
|                                              | OfferFixIt                      | `true`        | 수정 제안(Fix-It) 제공 여부                           |
| **ast-extern-function-declaration**          | OnlyMainFile                    | `true`        | 메인 파일만 검사할지 여부                                |
|                                              | IgnoreSystemHeaders             | `true`        | 시스템 헤더 무시 여부                                  |
|                                              | IgnoreMacroExpansions           | `true`        | 매크로 확장 결과 무시 여부                               |
|                                              | RequireExplicitExtern           | `false`       | 외부 함수 선언에 `extern` 키워드를 명시하도록 강제할지 여부         |
| **ast-function-call-argument-consistency**   | OnlyMainFile                    | `false`       | 메인 파일만 검사할지 여부                                |
|                                              | IgnoreSystemHeaders             | `true`        | 시스템 헤더 무시 여부                                  |
|                                              | IgnoreMacroExpansions           | `true`        | 매크로 확장 결과 무시 여부                               |
|                                              | EnforceExactParamType           | `true`        | 정확한 파라미터 타입 일치 요구 여부                          |
|                                              | AllowArrayToPointerDecay        | `true`        | 배열에서 포인터로의 암묵적 변환 허용 여부                       |
|                                              | AllowFunctionToPointerDecay     | `true`        | 함수에서 함수 포인터로의 암묵적 변환 허용 여부                    |
|                                              | AllowQualificationUpcast        | `true`        | `T*` -> `const T*` 등 한정자 추가 암묵 변환 허용 여부       |
|                                              | AllowDerivedToBase              | `true`        | 파생 클래스에서 기본 클래스로의 암묵적 변환 허용 여부(C++)           |
|                                              | AllowNullToPointer              | `true`        | `0`/`nullptr`에서 포인터로의 암묵적 변환 허용 여부            |
|                                              | AllowIntegralPromotion          | `false`       | 정수 승격(char->int 등) 암묵 변환 허용 여부                |
|                                              | AllowFloatingPromotion          | `false`       | 부동소수점 승격(float->double 등) 암묵 변환 허용 여부         |
|                                              | IgnoreExplicitCasts             | `false`       | 명시적 캐스트 무시 여부                                 |
|                                              | RequirePrototypeForCalls        | `true`        | 함수 호출 시 프로토타입 선언 요구 여부 (주로 C)                 |
| **ast-function-signature-consistency**       | OnlyMainFile                    | `false`       | 메인 파일만 검사할지 여부                                |
|                                              | IgnoreSystemHeaders             | `true`        | 시스템 헤더 무시 여부                                  |
|                                              | IgnoreMacroExpansions           | `true`        | 매크로 확장 결과 무시 여부                               |
| **ast-if-style**                             | OnlyMainFile                    | `true`        | 메인 파일만 검사할지 여부                                |
|                                              | IgnoreSystemHeaders             | `true`        | 시스템 헤더 무시 여부                                  |
|                                              | IgnoreMacroExpansions           | `true`        | 매크로 확장 결과 무시 여부                               |
| **ast-malloc-size-multiple**                 | OnlyMainFile                    | `true`        | 메인 파일만 검사할지 여부                                |
|                                              | IgnoreSystemHeaders             | `true`        | 시스템 헤더 무시 여부                                  |
|                                              | IgnoreMacroExpansions           | `true`        | 매크로 확장 결과 무시 여부                               |
|                                              | RequireExplicitSizeof           | `false`       | `malloc` 크기가 상수여도 `sizeof(T)` 사용을 강제할지 여부     |
|                                              | AllowFlexibleTail               | `true`        | `sizeof(T) + tail` 형태의 크기 허용 여부 (유연한 배열 멤버 등) |
| **ast-multi-statement-per-line**             | OnlyMainFile                    | `true`        | 메인 파일만 검사할지 여부                                |
|                                              | IgnoreSystemHeaders             | `true`        | 시스템 헤더 무시 여부                                  |
|                                              | IgnoreMacroExpansions           | `true`        | 매크로 확장 결과 무시 여부                               |
|                                              | CheckGlobalDecls                | `true`        | 전역 선언 검사 여부                                   |
|                                              | CheckFunctionBodies             | `true`        | 함수 본문 검사 여부                                   |
|                                              | CheckCompoundStmts              | `true`        | 복합 명령문(블록) 검사 여부                              |
|                                              | CheckRecordMembers              | `true`        | 레코드(클래스/구조체) 멤버 검사 여부                         |
| **ast-no-assignment-in-condition**           | OnlyMainFile                    | `true`        | 메인 파일만 검사할지 여부                                |
|                                              | IgnoreSystemHeaders             | `true`        | 시스템 헤더 무시 여부                                  |
|                                              | IgnoreMacroExpansions           | `true`        | 매크로 확장 결과 무시 여부                               |
|                                              | CheckTernaryCondition           | `true`        | 삼항 연산자(?:)의 조건식도 검사할지 여부                      |
| **ast-no-auto-type**                         | OnlyMainFile                    | `true`        | 메인 파일만 검사할지 여부                                |
|                                              | IgnoreSystemHeaders             | `true`        | 시스템 헤더 무시 여부                                  |
|                                              | IgnoreMacroExpansions           | `true`        | 매크로 확장 결과 무시 여부                               |
|                                              | CheckVariableDecls              | `true`        | 변수 선언 시 `auto` 사용 검사 여부                       |
|                                              | CheckFunctionReturns            | `true`        | 함수 반환 타입에 `auto` 사용 검사 여부 (C++14 이상)          |
| **ast-no-float-equality**                    | OnlyMainFile                    | `true`        | 메인 파일만 검사할지 여부                                |
|                                              | IgnoreSystemHeaders             | `true`        | 시스템 헤더 무시 여부                                  |
|                                              | IgnoreMacroExpansions           | `false`       | 매크로 확장 결과 무시 여부                               |
|                                              | IncludeDouble                   | `true`        | `double` 타입 포함 여부                             |
|                                              | IncludeLongDouble               | `true`        | `long double` 타입 포함 여부                        |
|                                              | RequireBothOperandsFloating     | `false`       | 양쪽 피연산자 모두 부동소수점일 때만 검사할지 여부                  |
|                                              | IgnoreBothConstantOperands      | `false`       | 양쪽 피연산자 모두 상수일 때 무시할지 여부                      |
|                                              | AllowNaNSelfInequalityPattern   | `false`       | `x != x` 형태의 NaN 검사 패턴 허용 여부                  |
|                                              | AllowCompareWithZero            | `false`       | 0과의 비교 허용 여부                                  |
|                                              | OfferFixItUsingEpsilon          | `false`       | Epsilon을 사용한 수정 제안(Fix-It) 제공 여부              |
|                                              | EpsilonExpr                     | `"EPS"`       | Fix-It에 사용할 Epsilon 표현식                       |
|                                              | FixFunction                     | `"std::fabs"` | Fix-It에 사용할 절대값 함수                            |
| **ast-no-goto**                              | OnlyMainFile                    | `true`        | 메인 파일만 검사할지 여부                                |
|                                              | IgnoreSystemHeaders             | `true`        | 시스템 헤더 무시 여부                                  |
|                                              | IgnoreMacroExpansions           | `true`        | 매크로 확장 결과 무시 여부                               |
| **ast-no-meaningless-expr**                  | OnlyMainFile                    | `true`        | 메인 파일만 검사할지 여부                                |
|                                              | IgnoreSystemHeaders             | `true`        | 시스템 헤더 무시 여부                                  |
|                                              | IgnoreMacroExpansions           | `true`        | 매크로 확장 결과 무시 여부                               |
| **ast-no-mixed-char-wide-string**            | OnlyMainFile                    | `true`        | 메인 파일만 검사할지 여부                                |
|                                              | IgnoreSystemHeaders             | `true`        | 시스템 헤더 무시 여부                                  |
|                                              | IgnoreMacroExpansions           | `true`        | 매크로 확장 결과 무시 여부                               |
| **ast-no-mixed-linkage**                     | OnlyMainFile                    | `true`        | 메인 파일만 검사할지 여부                                |
|                                              | IgnoreSystemHeaders             | `true`        | 시스템 헤더 무시 여부                                  |
|                                              | IgnoreMacroExpansions           | `true`        | 매크로 확장 결과 무시 여부                               |
| **ast-no-out-of-range-assignment**           | OnlyMainFile                    | `true`        | 메인 파일만 검사할지 여부                                |
|                                              | IgnoreSystemHeaders             | `true`        | 시스템 헤더 무시 여부                                  |
|                                              | IgnoreMacroExpansions           | `true`        | 매크로 확장 결과 무시 여부                               |
|                                              | OnlyConstantExpr                | `true`        | 상수 표현식만 검사할지 여부                               |
|                                              | CheckBitFields                  | `true`        | 비트 필드 검사 여부                                   |
|                                              | CheckBoolean                    | `false`       | `bool` 타입 대상 포함 여부                            |
|                                              | CheckFloatingTargets            | `true`        | 부동소수점 대상 검사 여부                                |
|                                              | DiagnoseFloatUnderflow          | `false`       | 부동소수점 언더플로우 진단 여부                             |
|                                              | DiagnoseFloatInexact            | `false`       | 부동소수점 부정확(반올림 등) 진단 여부                        |
| **ast-no-shadowing**                         | OnlyMainFile                    | `true`        | 메인 파일만 검사할지 여부                                |
|                                              | IgnoreSystemHeaders             | `true`        | 시스템 헤더 무시 여부                                  |
|                                              | IgnoreMacroExpansions           | `true`        | 매크로 확장 결과 무시 여부                               |
|                                              | ForbidOuterLocalShadowing       | `true`        | 바깥 지역 변수 섀도잉 금지 여부                            |
|                                              | ForbidGlobalShadowing           | `true`        | 전역 변수 섀도잉 금지 여부                               |
|                                              | CheckParamShadowing             | `true`        | 함수 파라미터 섀도잉 검사 여부                             |
|                                              | CheckCatchParamShadowing        | `true`        | `catch` 파라미터 섀도잉 검사 여부                        |
|                                              | CheckStructuredBindings         | `true`        | 구조화 바인딩 섀도잉 검사 여부 (C++17)                     |
|                                              | OnlyMainFileGlobals             | `true`        | 메인 파일의 전역 변수만 섀도잉 대상으로 할지 여부                  |
|                                              | IgnoreForLoopInit               | `false`       | `for` 루프 초기화 변수 무시 여부                         |
|                                              | IgnoreIfSwitchInit              | `false`       | `if/switch` 초기화 변수 무시 여부 (C++17)              |
|                                              | IncludeMethods                  | `true`        | 클래스 메서드 내부 검사 여부                              |
|                                              | IncludeFreeFunctions            | `true`        | 자유 함수 내부 검사 여부                                |
|                                              | OfferRenameFixIt                | `true`        | 이름 변경 수정 제안(Fix-It) 제공 여부                     |
|                                              | RenameSuffix                    | `"_new"`      | Fix-It 제안 시 추가할 접미사                           |
| **ast-pointer-cv-qualifier-drop**            | OnlyMainFile                    | `true`        | 메인 파일만 검사할지 여부                                |
|                                              | IgnoreSystemHeaders             | `true`        | 시스템 헤더 무시 여부                                  |
|                                              | IgnoreMacroExpansions           | `true`        | 매크로 확장 결과 무시 여부                               |
|                                              | WarnOnConstDrop                 | `true`        | `const` 한정자 제거 시 경고 여부                        |
|                                              | WarnOnVolatileDrop              | `true`        | `volatile` 한정자 제거 시 경고 여부                     |
| **ast-representable-cast**                   | OnlyMainFile                    | `true`        | 메인 파일만 검사할지 여부                                |
|                                              | IgnoreSystemHeaders             | `true`        | 시스템 헤더 무시 여부                                  |
|                                              | IgnoreMacroExpansions           | `true`        | 매크로 확장 결과 무시 여부                               |
|                                              | WarnPotential                   | `true`        | 잠재적 위험도 경고할지 여부                               |
|                                              | IgnoreExplicitCasts             | `false`       | 명시적 캐스트 무시 여부                                 |
|                                              | TreatEnumAsUnderlying           | `true`        | `enum`을 기저 정수 타입으로 간주하고 검사할지 여부               |
|                                              | WarnIntToFloat                  | `true`        | 정수에서 실수로 변환 시 정밀도 손실 가능성 경고 여부                |
| **ast-sequencing-rules**                     | OnlyMainFile                    | `true`        | 메인 파일만 검사할지 여부                                |
|                                              | IgnoreMacroExpansions           | `true`        | 매크로 확장 결과 무시 여부                               |
|                                              | IgnoreSystemHeaders             | `true`        | 시스템 헤더 무시 여부                                  |
|                                              | TrackMembersAndSubscripts       | `false`       | 멤버 접근(`a.b`)이나 배열 첨자(`a[i]`)도 추적할지 여부         |
| **ast-shift-amount-range**                   | OnlyMainFile                    | `true`        | 메인 파일만 검사할지 여부                                |
|                                              | IgnoreSystemHeaders             | `true`        | 시스템 헤더 무시 여부                                  |
|                                              | IgnoreMacroExpansions           | `true`        | 매크로 확장 결과 무시 여부                               |
|                                              | UseDeclaredFixedWidth           | `true`        | 선언된 원본 타입의 비트 폭을 우선 사용할지 여부                   |
|                                              | WarnOnBoolShift                 | `true`        | `bool` 타입에 대한 시프트 연산 경고 여부                    |
| **ast-signed-bitwise-safety**                | OnlyMainFile                    | `true`        | 메인 파일만 검사할지 여부                                |
|                                              | IgnoreSystemHeaders             | `true`        | 시스템 헤더 무시 여부                                  |
|                                              | IgnoreMacroExpansions           | `true`        | 매크로 확장 결과 무시 여부                               |
|                                              | WarnOnShiftLeftSigned           | `true`        | 부호 있는 타입에 `<<` 사용 시 경고 여부                     |
|                                              | WarnOnBitwiseNotSigned          | `true`        | 부호 있는 타입에 `~` 사용 시 경고 여부                      |
|                                              | WarnOnBitwiseAndOrXor           | `true`        | 부호 있는 타입에 `&`, `                              |
|                                              | WarnOnShiftCountUB              | `true`        | 시프트 양이 음수 또는 비트 폭 이상일 때 경고 여부                 |
|                                              | AllowIfGuardNonNegative         | `false`       | `if (x >= 0)` 가드가 있으면 `<<` 허용 여부              |
|                                              | AllowExplicitUnsignedCast       | `true`        | 명시적 `unsigned` 캐스트가 있으면 허용 여부                 |
| **ast-single-exit-and-return-type**          | OnlyMainFile                    | `true`        | 메인 파일만 검사할지 여부                                |
|                                              | IgnoreSystemHeaders             | `true`        | 시스템 헤더 무시 여부                                  |
|                                              | IgnoreMacroExpansions           | `true`        | 매크로 확장 결과 무시 여부                               |
|                                              | CheckSingleExit                 | `true`        | 함수당 `return`문이 하나인지 검사 여부                     |
|                                              | CheckReturnType                 | `true`        | 반환 타입 일치 검사 여부                                |
|                                              | CheckMissingReturn              | `true`        | `return`문 누락 검사 여부                            |
| **ast-switch-style**                         | OnlyMainFile                    | `true`        | 메인 파일만 검사할지 여부                                |
|                                              | IgnoreSystemHeaders             | `true`        | 시스템 헤더 무시 여부                                  |
|                                              | IgnoreMacroExpansions           | `true`        | 매크로 확장 결과 무시 여부                               |
|                                              | CheckLogicalOperators           | `true`        | `switch` 조건식에 논리 연산자 사용 검사 여부                 |
|                                              | RequireDefaultLabel             | `true`        | `default` 레이블 필수 여부                           |
|                                              | CheckCodeBeforeFirstLabel       | `true`        | 첫 `case`/`default` 전에 코드가 있는지 검사 여부           |
|                                              | CheckFallThrough                | `false`       | `case` 끝에 `break` 등이 없는 fall-through 검사 여부    |
|                                              | CheckUnreachableCases           | `true`        | 도달 불가능한 `case` 검사 여부                          |
|                                              | WarnOnEnumMismatch              | `true`        | `enum` 타입 `switch`에서 정의되지 않은 `case` 값 경고 여부   |
|                                              | CheckComplexExpressions         | `false`       | `switch` 조건식의 복잡성 검사 여부 (현재 구현에서는 사용 안 함)     |
| **ast-unevaluated-side-effect**              | OnlyMainFile                    | `true`        | 메인 파일만 검사할지 여부                                |
|                                              | IgnoreSystemHeaders             | `true`        | 시스템 헤더 무시 여부                                  |
|                                              | IgnoreMacroExpansions           | `true`        | 매크로 확장 결과 무시 여부                               |
|                                              | CheckSizeofAlignof              | `true`        | `sizeof`/`alignof` 내부 부작용 검사 여부               |
|                                              | CheckNoexcept                   | `true`        | `noexcept` 내부 부작용 검사 여부                       |
|                                              | CheckDecltype                   | `true`        | `decltype` 내부 부작용 검사 여부                       |
| **ast-unsigned-minus-assignment**            | OnlyMainFile                    | `true`        | 메인 파일만 검사할지 여부                                |
|                                              | IgnoreSystemHeaders             | `true`        | 시스템 헤더 무시 여부                                  |
|                                              | IgnoreMacroExpansions           | `true`        | 매크로 확장 결과 무시 여부                               |
|                                              | CheckAssignment                 | `true`        | 단순 대입(`=`) 검사 여부                              |
|                                              | CheckCompoundAssignments        | `false`       | 복합 대입(`+=`, `-=` 등) 검사 여부                     |
|                                              | CheckVarInit                    | `true`        | 변수 선언 초기화 검사 여부                               |
|                                              | CheckReturn                     | `true`        | `return`문 검사 여부                               |
|                                              | CheckCtorMemberInit             | `true`        | 생성자 멤버 초기화 검사 여부                              |
|                                              | AllowExplicitUnsignedCast       | `false`       | 명시적 `unsigned` 캐스트가 있으면 허용할지 여부               |
| **ast-varargs-format**                       | OnlyMainFile                    | `true`        | 메인 파일만 검사할지 여부                                |
|                                              | IgnoreSystemHeaders             | `true`        | 시스템 헤더 무시 여부                                  |
|                                              | IgnoreMacroExpansions           | `true`        | 매크로 확장 결과 무시 여부                               |
|                                              | EnablePrintf                    | `true`        | `printf` 계열 함수 검사 여부                          |
|                                              | EnableScanf                     | `true`        | `scanf` 계열 함수 검사 여부                           |
|                                              | AllowDynamicFormat              | `false`       | 포맷 문자열이 상수가 아닐 때 허용 여부                        |
|                                              | HonorMSVCExt                    | `false`       | MSVC 확장 포맷 지정자(`%I32d` 등) 지원 여부               |
|                                              | WarnOnPercentN                  | `true`        | `%n` 포맷 지정자 사용 시 경고 여부                        |
|                                              | StrictWCharFamily               | `false`       | `wprintf` 등 wide 함수 계열에서 wide 리터럴만 허용할지 여부    |
|                                              | ScanfRequirePointerExact        | `true`        | `scanf`에서 정확한 포인터 타입 요구 여부                    |
|                                              | PrintfZeroWidthStarAsInt        | `true`        | `printf`의 `*` 너비/정밀도에 `int` 타입 요구 여부          |
| **cfg-init-before-use**                      | OnlyMainFile                    | `true`        | 메인 파일만 검사할지 여부                                |
|                                              | IgnoreSystemHeaders             | `true`        | 시스템 헤더 무시 여부                                  |
|                                              | IgnoreMacroExpansions           | `true`        | 매크로 확장 결과 무시 여부                               |
|                                              | TreatMemsetZeroAsInit           | `false`       | `memset(&x, 0, ...)`을 초기화로 간주할지 여부            |
|                                              | TreatCtorAsInitForClass         | `true`        | 비-trivial 기본 생성자를 가진 클래스/구조체를 초기화된 것으로 간주할지   |
|                                              | WarnOnMaybeUninit               | `false`       | 초기화되지 않았을 '가능성'이 있는 경우도 경고할지 여부               |
| **cfg-nonzero-divisor-guard**                | OnlyMainFile                    | `true`        | 메인 파일만 검사할지 여부                                |
|                                              | IgnoreSystemHeaders             | `true`        | 시스템 헤더 무시 여부                                  |
|                                              | IgnoreMacroExpansions           | `true`        | 매크로 확장 결과 무시 여부                               |
|                                              | CheckModulus                    | `true`        | 나머지 연산자(`%`, `%=`)도 검사할지 여부                   |
|                                              | CheckCompoundAssignment         | `true`        | 복합 대입(`/=`, `%=`)도 검사할지 여부                    |
|                                              | IncludeFloatingPoint            | `true`        | 부동소수점 분모도 검사할지 여부                             |
|                                              | RecognizeAssertAsGuard          | `true`        | `assert(d != 0)` 등을 가드로 인식할지 여부               |
|                                              | RecognizeConstNonZeroAsGuard    | `true`        | 상수로 0이 아닌 값 대입을 가드로 인식할지 여부                   |
|                                              | RecognizeSizeofAsNonZero        | `true`        | `sizeof(...)` 대입을 0이 아닌 값으로 인식할지 여부           |
|                                              | CfgBlockVisitBudget             | `4000U`       | CFG 분석 시 최대 방문 블록 수                           |
| **cfg-null-dereference-guard**               | OnlyMainFile                    | `true`        | 메인 파일만 검사할지 여부                                |
|                                              | IgnoreSystemHeaders             | `true`        | 시스템 헤더 무시 여부                                  |
|                                              | IgnoreMacroExpansions           | `true`        | 매크로 확장 결과 무시 여부                               |
|                                              | CheckArraySubscript             | `true`        | 배열 인덱싱(`p[i]`)도 역참조로 간주할지 여부                  |
|                                              | TreatAddressOfInitAsNonNull     | `true`        | 변수 주소(`&x`) 대입/초기화를 Non-NULL로 간주할지 여부         |
|                                              | TreatNewAsNonNull               | `true`        | `new` 표현식 결과를 Non-NULL로 간주할지 여부 (예외 모드 가정)    |
|                                              | RecognizeAssertAsGuard          | `true`        | `assert(p != NULL)` 등을 가드로 인식할지 여부            |
|                                              | CfgBlockVisitBudget             | `4000U`       | CFG 분석 시 최대 방문 블록 수                           |
|                                              | AssumeNonNullParams             | `false`       | 함수 파라미터를 기본적으로 Non-NULL로 가정할지 여부              |
|                                              | AssumeNonNullGlobals            | `false`       | 전역 변수를 기본적으로 Non-NULL로 가정할지 여부                |
| **cfg-null-pointer-arithmetic**              | OnlyMainFile                    | `true`        | 메인 파일만 검사할지 여부                                |
|                                              | IgnoreSystemHeaders             | `true`        | 시스템 헤더 무시 여부                                  |
|                                              | IgnoreMacroExpansions           | `true`        | 매크로 확장 결과 무시 여부                               |
|                                              | CheckArraySubscriptBase         | `true`        | 배열 인덱싱(`p[i]`)의 베이스 포인터 검사 여부                 |
|                                              | GuardScanBudget                 | `400U`        | NULL 보장 경로 탐색 시 최대 CFG 블록 수                   |
|                                              | ConsiderInitNull                | `true`        | NULL 초기화 후 재대입 없이 사용 시 경고 여부 (휴리스틱)           |
| **cfg-uninitialized-address-to-const-param** | OnlyMainFile                    | `true`        | 메인 파일만 검사할지 여부                                |
|                                              | IgnoreSystemHeaders             | `true`        | 시스템 헤더 무시 여부                                  |
|                                              | IgnoreMacroExpansions           | `true`        | 매크로 확장 결과 무시 여부                               |
|                                              | ConsiderMemsetZeroAsInit        | `true`        | `memset(&x, 0, ...)`을 초기화로 간주할지 여부            |
|                                              | ConsiderCompoundAssignAsInit    | `false`       | 복합 대입(`+=` 등)을 초기화로 간주할지 여부                   |
|                                              | CfgBlockVisitBudget             | `2000U`       | CFG 분석 시 최대 방문 블록 수                           |
| **cfg-unreachable-code**                     | OnlyMainFile                    | `true`        | 메인 파일만 검사할지 여부                                |
|                                              | IgnoreSystemHeaders             | `true`        | 시스템 헤더 무시 여부                                  |
|                                              | IgnoreMacroExpansions           | `true`        | 매크로 확장 결과 무시 여부                               |